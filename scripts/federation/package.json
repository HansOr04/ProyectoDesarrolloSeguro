{
  "name": "federation-scripts",
  "version": "1.0.0",
  "description": "Scripts de federación de usuarios hacia Keycloak",
  "scripts": {
    "migrate": "node migrate-users.js",
    "migrate:dry": "node migrate-users.js --dry-run",
    "test": "jest --no-coverage"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "mongodb": "^6.3.0",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
